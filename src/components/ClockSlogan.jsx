const ClockSlogan = () => {
  return (
    <div className="clock-slogan">
      <p
        className="fw-bold d-flex justify-content-center text-white"
        style={{ letterSpacing: "0.1em" }}
      >
        "Time is a precious resource, and in Pakistan, we value every moment.
        Every second counts — for a brighter, stronger Pakistan."
      </p>
    </div>
  );
};
export default ClockSlogan;
